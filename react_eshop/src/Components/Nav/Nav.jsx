import { NavLink } from "react-router-dom";

function Nav() {
    return (
        <nav>
            <div>
                <NavLink to="/">Home</NavLink>
            </div>
        </nav>
    );
}

export default Nav;
